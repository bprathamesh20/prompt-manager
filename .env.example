# Docker Compose host port bindings (change these to avoid local conflicts)
POSTGRES_PORT=5432
BACKEND_PORT=8000
FRONTEND_PORT=5173

# Frontend runtime API base URL used by the frontend container
VITE_API_BASE_URL=http://localhost:8000
